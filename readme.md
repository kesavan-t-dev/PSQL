**​**

According to your pandas task,

-   You have an opportunities to recall SQL basics.
-   Write 15 - 20 SQL queries with your tables.
-   Should have queries with various SQL functions. Such as, string, numeric, date, aggregate functions.
-   Should have joins, typecasting
-   Then write code in Python using Pandas to get same results which are given by the SQL queries. Should not use SQL queries in python, Except fetching source tables.